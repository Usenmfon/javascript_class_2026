let numbers = [
    [0, 1, 2, 4],
    [3, 4, 5, 9],
    [6, 7, 8, 0],
    [6, 7, 8, 0],
    [6, 7, 8, 0],
    [6, 7, 8, 0],
]

for(let i = 0; i < numbers.length; i++)
{
    for(let j = 0; j < numbers[i].length; j++)
    {
        // console.log(numbers[i][j])
    }
}


// numbers.forEach((number) => number.forEach((x) => console.log(x)))

let new_numbers = [0, 1, 3];

// let allNums = new_numbers.map(x => x * 2);

new_numbers.forEach(x => console.log(x * 2));
// console.log(new_numbers)
// console.log(allNums)


// console.log(numbers)

// console.log(numbers[2][1])

let fruits = ['mango', 'apple', 'orange']

// console.log(fruits[1])


